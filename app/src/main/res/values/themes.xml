<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FlashCourse" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FlashCourse" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Definisikan warna terang -->
        <item name="colorPrimary">@color/primary_light</item>
        <item name="colorPrimaryDark">@color/primary_dark_light</item>
        <item name="colorAccent">@color/accent_light</item>
        <!-- Warna latar belakang untuk mode terang -->
        <item name="android:windowBackground">@color/background_light</item>
    </style>
</resources>
